# Legacy Phase 1A Downstream Configs

The YAML files in this directory are the old phase1a raw-signal/tokenizer-classifier configs. They were moved out of `experiments/configs/phase1a/` on 2026-06-11 to avoid confusion with the current source/observation token-sequence downstream workflow.

Current token export config:

- `experiments/configs/downstream/source_observation_token_export_coupling0.yaml`
