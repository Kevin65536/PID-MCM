_base_: "./gate1_health_uniform32_stable_sourceonly_balance_t2_bs128.yaml"

experiment:
  name: "s2_phase1_gate1_health_uniform32_stable_sourceonly_balance_obsramp_bs128"
  description: "Phase 1 Gate1 source-only balance base with delayed weak observation balance warmup"

loss:
  codebook:
    observation_assignment_temperature: 2.0

training:
  balance_warmup:
    observation:
      enabled: true
      start_epoch: 25
      ramp_epochs: 30
      start_scale: 0.0
      end_scale: 0.35
