_base_: "./gate1_health_uniform32_stable_sourceonly_balance_provq_bs128.yaml"

_base_: "./gate1_baseline_locked_bs128.yaml"

experiment:
  name: "s2_phase1_gate1_health_uniform32_stable_sourceonly_balance_provq_nophase_longwarmup_bs128"
  description: "Phase 1 Gate1 locked baseline without explicit phase loss, with longer training and slower quantization warmup"

training:
  epochs: 320
  warmup_epochs: 15
  early_stopping:
    patience: 160
  quantization_warmup:
    enabled: true
    start_epoch: 1
    ramp_epochs: 120
    start_scale: 0.0
    end_scale: 1.0