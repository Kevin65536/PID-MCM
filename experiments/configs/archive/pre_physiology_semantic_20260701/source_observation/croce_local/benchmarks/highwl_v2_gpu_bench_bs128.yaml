_base_: "../highwl_v2_branch_norm_structural_coupling_lr2e4_compile.yaml"

experiment:
  name: "bench_highwl_v2_bs128_full_loss"
  device: "cuda:1"
  run_group: "source_observation/croce_local/highwl_v2_benchmarks"
  description: "Short GPU throughput benchmark: active full-loss config, batch size 128."

training:
  epochs: 2
  batch_size: 128
  max_train_batches: 120
  checkpoint:
    save_every: 0
  early_stopping:
    enabled: false
  validation:
    interval_epochs: 1000
    start_epoch: 1000
    max_batches: 0

logging:
  tensorboard:
    enabled: false
