# Local BIOT checkpoints

Binary assets in this directory are local and ignored by the parent repository.
The pinned upstream checkout already carries the three official EEG checkpoints
under `../upstream/pretrained-models/`; see `../sources/method_manifest.yaml`
for immutable URLs and hashes.
