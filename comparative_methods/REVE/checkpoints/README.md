# Local REVE checkpoints

REVE encoder weights are gated by the authors' Responsible Use Agreement.
They must be obtained directly from Hugging Face after the user reviews and
accepts those terms. Do not commit or redistribute downloaded weights.

The ungated electrode position-bank artifact may also be kept here. See
`../sources/method_manifest.yaml` and `../scripts/fetch_weights.py`.
