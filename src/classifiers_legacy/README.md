# Legacy Classifiers

This package contains the previous raw-signal and online-tokenizer downstream classifiers. It is retained for archive reproducibility only.

Current source/observation downstream work should start from exported 2s token sequences produced by `experiments/scripts/export_source_observation_tokens.py`, then use `src/foundation` or a new token-sequence downstream model.
